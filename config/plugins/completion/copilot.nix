{
  config = {
    plugins.copilot-lua = {
      enable = true;
      # suggestion.enable = false;
      # panel.enable = false;
      # suggestion.keymap = {
      #   accept = 
      # };
    };
  };
}
